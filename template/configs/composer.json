{
	"name": "{{package.name}}",
	"description": "{{wp.description}}",
	"type": "wordpress-plugin",
	"license": "GPL-3.0",
	"authors": [
		{
			"name": "{{author.name}}",
			"email": "{{author.email}}",
			"homepage": "{{author.url}}"
		}
	],
	"require": {
		"php": ">=5.6"
	},
	"minimum-stability": "dev",
	"prefer-stable": true,
	"autoload" : {
		"classmap" : [ "includes/" ]
	},
	"config": {
		"optimize-autoloader": true
	}
}
